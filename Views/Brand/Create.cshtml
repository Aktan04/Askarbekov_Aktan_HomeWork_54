@model Brand

@{
    ViewBag.Title = "createBrand";
    Layout = "_Layout";
}

<h2>Заполните форму для добавления бренда</h2>

<div class="row">
    <div class="col-md-6">
        <form asp-action="Create" method="post">
            @Html.ValidationSummary(true)
            <div class="form-group">
                <label asp-for="NameOfBrand" class="fs-2">Наименование</label>
                <input asp-for="NameOfBrand"  class="form-control" placeholder="Введите наименование">
                <span asp-validation-for="NameOfBrand" class="text-danger"></span>
            </div>
            <button type="submit" class="btn btn-primary mt-2">Создать</button>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}