@model Brand
@{
    ViewBag.Title = "EditBrand";
    Layout = "_Layout";
}

<h2>Редактирование бренда</h2>

<div class="row">
    <div class="col-md-6">
        <form asp-action="Edit" asp-controller="Brand" method="post">
            @Html.ValidationSummary(true)
            <input asp-for="Id" type="hidden"/>
            <div class="form-group">
                <label asp-for="NameOfBrand"></label>
                <input asp-for="NameOfBrand" class="form-control"/>
                <span asp-validation-for="NameOfBrand" class="text-danger"></span>
            </div>
            <button type="submit" class="btn btn-primary">Изменить</button>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}